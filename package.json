{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "lerna run start --stream --parallel",
    "build": "lerna run build --stream"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
